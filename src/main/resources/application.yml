# src/main/resources/application.yml для cargo-service

server:
  port: 8085

spring:
  profiles:
    active: dev

  application:
    name: be-logistics-platform-cargo-service

  # DataSource для PostgreSQL (Единая общеплатформенная БД)
  datasource:
    url: jdbc:postgresql://localhost:5433/logistics_platform_db?currentSchema=cargo_schema # 👈 Изменили порт на 5433, база данных и схема сохранены прежние
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jackson:
    serialization: # чтобы Jackson правильно сериализует/десериализует.
      write-dates-as-timestamps: false
    time-zone: Europe/Moscow
    # 👇 Формат вывода дат в JSON для клиентов
    date-format: "yyyy-MM-dd HH:mm:ss"

  # JPA / Hibernate базовые настройки
  jpa:
#    hibernate:
#      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
          time_zone: Europe/Moscow          # 👈 GMT+3
        default_schema: cargo_schema
    open-in-view: false

  # Общий Liquibase
  liquibase:
    enabled: true
#    enabled: false   # временно отключаем Liquibase
    change-log: classpath:db/changelog/db.changelog-master.xml
    default-schema: cargo_schema
#    rollback-file: classpath:db/changelog/rollback.sql  # файл для SQL отката
    parameters:
      timezone: Europe/Moscow          # 👈 GMT+3 (влияет на даты в changelog-файлах)

  # Настройки баз данных Spring Data
  data:
    mongodb:
      # 👈 Внедряем специфичную переменную с дефолтным значением для логистики грузов
      uri: ${LOGISTICS_CARGO_MONGO_URI:mongodb://localhost:27018/logistics_cargo_mongo_db}

  # ============================================================
  # 🔐 SPRING SECURITY
  # ============================================================

  security:
    oauth2:
      resourceserver:
        jwt:
#          issuer-uri: http://localhost:8080/realms/logistics-platform
          issuer-uri: ${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI:http://localhost:8080/realms/logistics-platform}

  # 🟢 Public endpoints (не требуют авторизации)
  public:
    endpoints:
      - /actuator/health
      - /actuator/info
      - /swagger-ui/**
      - /v3/api-docs/**

logging:
  level:
    com.innowise.logistics.cargoservice: DEBUG
    liquibase: INFO
  pattern:
    # 👇 Красивый формат вывода времени, без миллисекунд
    dateformat: "yyyy-MM-dd HH:mm:ss"
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n"
